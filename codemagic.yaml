workflows:
  ios:
    name: iOS Build
    environment:
      flutter: stable
    scripts:
      - name: Install dependencies
        script: |
          flutter pub get
          cd ios
          pod install
      - name: Build iOS app
        script: flutter build ios --release
    artifacts:
      - build/ios/iphoneos/Runner.ipa

